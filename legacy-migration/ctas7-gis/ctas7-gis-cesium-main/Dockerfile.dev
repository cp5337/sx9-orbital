# CTAS Primary GIS System - Development Docker Image
FROM node:20-alpine

# Install system dependencies
RUN apk add --no-cache \
    git \
    python3 \
    make \
    g++

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies with overrides applied
RUN npm ci

# Copy source code
COPY . .

# Expose Vite dev server port
EXPOSE 25173

# Start development server
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
